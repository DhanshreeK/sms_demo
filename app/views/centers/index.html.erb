<%= render 'layouts/messages'%>
<div class="row">
        <div class="col-lg-12">
        <br><br>
          <div class="panel panel-primary">
            <div class="panel-heading clearfix">
              <h3 class="panel-title">Center Details</h3>
              <ul class="panel-tool-options"> 
                <li><a data-rel="collapse" href="#"><i class="icon-down-open"></i></a></li>
                <li><a data-rel="reload" href="#"><i class="icon-arrows-ccw"></i></a></li>
                <li><a data-rel="close" href="#"><i class="icon-cancel"></i></a></li>
              </ul>
            </div>
            <div class="panel-body">
            <%= link_to 'New Center', new_center_path, class: 'btn btn-link'%>
              <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover dataTables-example" >
<p id="notice"><%= notice %></p>
  <thead>
    <tr>
      <th>Center name</th>
      <th>Address</th>
      <th>City</th>
      <th>State</th>
      <th>Pan card no</th>
      <th>Contact</th>
      <th>Alternate contact</th>
      <th>Center starting date</th>
      <th>Contact person name</th>
      <th>Residential address</th>
      <th>Center code</th>
      <th>Country</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @centers.each do |center| %>
      <tr>
        <td><%= center.center_name %></td>
        <td><%= center.address %></td>
        <td><%= center.city %></td>
        <td><%= center.state %></td>
        <td><%= center.pan_card_no %></td>
        <td><%= center.contact %></td>
        <td><%= center.alternate_contact %></td>
        <td><%= center.center_starting_date %></td>
        <td><%= center.contact_person_name %></td>
        <td><%= center.residential_address %></td>
        <td><%= center.center_code %></td>
        <td><%= center.countries %></td>
        <td><%= center.center_status %></td> 
        <td><%= link_to 'Show', center ,class: "btn btn-blue btn-sm"%></td>
        <td><%= link_to 'Edit', edit_center_path(center),class: "btn btn-success btn-sm"%></td></i>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>
</div>
</div>
</div>